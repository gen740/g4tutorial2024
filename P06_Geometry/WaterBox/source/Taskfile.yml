# https://taskfile.dev

version: '3'

vars:
  GREETING: Hello, World!

tasks:
  cmake:
    cmds:
      - cmake -B build -S . -GNinja
    generates:
      - ./build/CMakeCache.txt
    sources:
      - ./CMakeLists.txt
  build:
    cmds:
      - task: cmake
      - cmake --build build
  run:
    cmds:
      - task: build
      - ./build/Application_Main
